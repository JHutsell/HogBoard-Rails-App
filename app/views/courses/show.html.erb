<h1><%= @course.name %></h1>
<h3>Taught by: <%= @course.professor.name %></h3>

<h4>Class Description: </h4>
<p><%= @course.description %></p>
<p>Class Roster: </p>
<ul>
    <% @students.each do |student| %>
        <li>
            <%= link_to student.name, student_path(student) %>
        </li>
    <% end %>
</ul>